node_modules/
dist/

# Verification debug artifact (regenerated by `npm run verify`)
scripts/.verify-screenshot.png

# Generated data — reproducible via scripts/fetch_overpass.mjs / build_from_pbf.sh.
# The compact central-core tileset is committed so the app works out of the box;
# the full municipality set and raw cache are ignored.
public/tiles/cache/
data/cache/

# Generated character asset — reproducible via scripts/fetch_character.mjs
public/assets/*.glb
